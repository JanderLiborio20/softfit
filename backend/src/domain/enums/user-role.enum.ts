/**
 * Papéis/Perfis de usuário no sistema
 */
export enum UserRole {
  CLIENT = 'client', // Cliente que usa o app
  NUTRITIONIST = 'nutritionist', // Nutricionista profissional
  ADMIN = 'admin', // Administrador do sistema (futuro)
}
